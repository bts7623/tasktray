# 개인 업무 스케줄 관리 프로그램(가칭 "TaskTray") 요구사항 정의서

| 항목 | 내용 |
|------|------|
| 문서 버전 | v1.2 |
| 작성일 | 2026-07-24 (v1.2 개정: 2026-07-29) |
| 용도 | VSCode + Claude Code 개발 지시용 요구사항 정의서 |
| 대상 플랫폼 | Windows 10/11 (64bit) |

> **v1.2 변경 이력**: 데이터 저장 위치를 "사용자 폴더 지정" 방식에서 **앱 자동 관리(`%APPDATA%\TaskTray` 고정)** 로 변경. 최초 실행 폴더 선택 다이얼로그를 제거하고, 환경설정의 [저장 폴더 열기] 버튼으로 접근한다. (FR-20/21/22, FR-24, 검수 7 개정)

---

## 1. 프로젝트 개요

### 1.1 목적
- 공식 업무 스케줄과 수시로 접수되는 업무를 즉시 메모하여 **업무 누락 방지**
- 완료한 업무를 사내 협업툴(flow SaaS)에 등록하기 전까지 안전하게 추적하여 **실적 등록 누락 방지**
- 축적된 완료 실적을 카테고리·시간순으로 내보내어 **연말 KPI·성과 정리에 활용**

### 1.2 배경
- 오며가며 혹은 급작스럽게 들어오는 요청 업무를 기억에만 의존하면 누락이 발생함
- 팀 업무는 flow SaaS로 등록·관리하는데, 일을 다 처리한 후 무엇을 했는지 기억나지 않아 flow 등록을 누락 → 실적 누락으로 이어짐
- 따라서 ① 수시 업무를 간편·경량으로 등록하고 ② 완료 업무가 flow에 등록 완료될 때까지 상태를 관리하며 ③ 누적 실적을 손쉽게 꺼내 쓸 수 있는 개인용 도구가 필요함

### 1.3 핵심 컨셉
- 메모장/스티커 메모처럼 **언제든 가볍게 켜고 끄는** 텍스트 기반 초경량 앱
- 시스템 트레이 상주형, 클릭 시 화면 우측에 메신저(카카오톡 PC버전) 크기의 세로형 패널 표시
- Task 1건 = 한 문장(약 20~30자) 수준의 짧은 텍스트
- 데이터는 로컬 JSON 파일 1개로 관리 → 타 PC 이동 시 JSON만 복사하면 됨

---

## 2. 개발 환경 및 기술 스택

### 2.1 기술 스택 (권장안)
| 구분 | 권장 | 사유 |
|------|------|------|
| 프레임워크 | **Tauri 2.x** (Rust backend + Web frontend) | 실행 파일·메모리 점유 최소화(경량 요구사항 충족), 트레이/자동시작/단일 인스턴스 공식 플러그인 제공, NSIS 설치파일 빌드 내장 |
| 프론트엔드 | TypeScript + React (또는 Vanilla TS) + Vite | 경량 UI, 설정 기반 테마 적용 용이 |
| 데이터 저장 | 로컬 JSON 파일 (`tasks.json`, `settings.json`) | DB 불필요, 이동성 확보 |
| 패키징 | Tauri bundler → NSIS 설치파일(.exe) | 설치형 배포 요구사항 충족 |

- 대안: Electron + electron-builder. 단, 배포 파일 크기와 메모리 점유가 커지므로 Tauri 구현이 곤란한 경우에만 채택할 것
- Claude Code는 개발 착수 전 위 스택으로 프로젝트를 초기화하고, 스택 변경이 필요하다고 판단되면 사유를 먼저 보고 후 진행할 것

### 2.2 개발·실행 환경
- 개발: Windows PC, VSCode + Claude Code
- 실행: Windows 10/11, 인터넷 연결 불필요(완전 오프라인 동작)
- 관리자 권한 없이 설치·실행 가능해야 함 (per-user 설치)

---

## 3. 용어 및 Task 상태 정의

### 3.1 용어
| 용어 | 정의 |
|------|------|
| Task | 사용자가 등록하는 업무 1건 (한 문장 수준의 텍스트) |
| 카테고리(category) | Task의 소속 업무 구분. 단일 텍스트 필드이나 구분자 `_`로 2뎁스 계층을 표현함 (예: `통합정보망_태양광`, `기관정보화_그룹웨어`) |
| 오늘 할 일(Pin) | 오늘 처리할 Task로 지정하여 목록 상단에 고정한 상태 (On/Off 토글) |
| flow | 팀 업무 등록용 사내 SaaS. 본 프로그램은 flow와 연동하지 않으며, "flow에 등록했는지 여부"만 체크리스트로 관리 |
| 종료(Archive) | flow 등록 완료 또는 제외 처리되어 기본 화면에서 숨겨진 상태. 데이터는 삭제되지 않고 JSON에 보존 |
| 실적 리포트 | 기간·조건을 지정하여 완료 Task를 카테고리 계층·시간순으로 내보낸 파일(Markdown/CSV). KPI 정리용 |

### 3.2 Task 상태 흐름 (상태 머신)
```
[등록: active]
   │  (완료 체크)
   ▼
[완료: done]  ← flow 등록 대기 목록에 표시
   │
   ├─ 'flow 등록 완료' 체크 → [종료: archived / flowStatus: registered]
   └─ '제외' 체크          → [종료: archived / flowStatus: excluded]
```
- `active` : 진행 중. 기본 목록에 표시. 오늘 할 일 Pin On/Off 가능
- `done` : 사용자가 완료 처리한 상태. "flow 등록 대기" 영역으로 이동하여 표시
- `archived` : flow 등록 완료 또는 제외 처리 완료. 기본 화면에서 숨김. 로우데이터 화면에서만 조회 가능
- 상태 전환 시 각 시각(timestamp)이 기록되어야 함
- `done` → `active` 되돌리기(완료 취소), `archived` → `done` 되돌리기 기능 제공 (오조작 복구용)

---

## 4. 기능 요구사항 (FR)

### 4.1 Task 관리
| ID | 요구사항 | 우선순위 |
|----|----------|:--:|
| FR-01 | Task 등록: 입력창에 텍스트 입력 후 Enter 또는 등록 버튼으로 즉시 등록. 등록 시 등록일시(createdAt) 자동 기록. 카테고리·종료예정일은 선택 입력 | 필수 |
| FR-02 | 카테고리 입력: 단일 텍스트 필드(선택 입력). 입력 시 **기존 Task들에 사용된 카테고리 값(전체 문자열 기준, 중복 제거)을 자동완성 목록으로 추천** (HTML datalist 또는 동등 기능). 예: "태" 입력 → "통합정보망_태양광" 추천 | 필수 |
| FR-03 | 카테고리 계층 규칙: 구분자는 `_`(언더스코어). 첫 번째 `_` 앞부분을 대분류, 뒷부분을 소분류로 해석. 구분자가 없으면 1뎁스(대분류만)로 취급. 별도의 카테고리 코드 관리 화면은 두지 않음(사용된 값 자체가 카테고리 목록이 됨) | 필수 |
| FR-04 | 제목 자동 분리(옵션): 제목 입력창에 `통합정보망_태양광_과업서 검토`처럼 입력 시 마지막 `_` 뒷부분을 제목, 앞부분을 카테고리로 자동 분리. 환경설정에서 On/Off 가능하며 **기본값 Off** | 권장 |
| FR-05 | Task 수정: 목록에서 해당 Task 클릭(또는 수정 아이콘)으로 제목·카테고리·종료예정일 수정 가능 | 필수 |
| FR-06 | Task 삭제: 개별 Task 삭제 가능. 삭제 전 확인(confirm) 1회. 삭제된 Task는 JSON에서 `deleted: true` 플래그 처리(soft delete)하여 로우데이터에서 추적 가능 | 필수 |
| FR-07 | 종료예정일(dueDate): Task별로 선택 입력 가능(미입력 허용). 날짜 초과 시 시각적 강조(예: 붉은색 표시) | 필수 |
| FR-08 | 오늘 할 일 Pin: Task별 On/Off 토글. On인 Task는 목록 최상단 고정 영역에 표시. 날짜가 지나도 사용자가 직접 Off 하기 전까지 유지(자동 해제 없음) | 필수 |
| FR-09 | 완료 처리: Task 체크 시 `done` 상태로 전환, 완료일시(completedAt) 기록, "flow 등록 대기" 영역으로 이동 | 필수 |
| FR-10 | flow 처리: `done` Task에 대해 [flow 등록 완료] / [제외] 중 하나 체크 → `archived` 전환 및 처리일시 기록, 기본 화면에서 숨김 | 필수 |
| FR-11 | 되돌리기: 완료 취소(done→active), 종료 취소(archived→done) 기능 제공 | 권장 |
| FR-12 | 정렬: 기본 정렬은 Pin 영역 → active → done. **Pin·active는 대분류(1depth) 가나다 → 소분류(2depth) 가나다 → task명 가나다 순**, done은 완료일 최신순. *(v1.2 개정: 기존 active 등록일 최신순 → 대분류·소분류·제목 가나다)* | 필수 |

### 4.2 로우데이터(Raw Data) 화면 및 실적 리포트
| ID | 요구사항 | 우선순위 |
|----|----------|:--:|
| FR-13 | 별도 화면(또는 탭)에서 archived·deleted 포함 전체 Task를 표 형태로 조회 가능 | 필수 |
| FR-14 | 로우데이터 화면 표시 항목: 제목, 카테고리, 상태, 등록일, 종료예정일, 완료일, flow 처리 구분(등록완료/제외), 삭제 여부 | 필수 |
| FR-15 | 필터: 상태별(전체/active/done/archived/deleted) + 카테고리별(대분류·전체값 기준) 필터 및 텍스트 검색 제공 | 필수 |
| FR-16 | JSON 파일이 저장된 폴더를 탐색기로 여는 버튼 제공 | 권장 |
| FR-17 | **실적 리포트 내보내기**: 로우데이터 화면에서 **완료일 기간(시작~종료)** 을 지정하여 파일로 내보내기. **KPI 총계는 flow 등록완료 기준**이며, **제외·미처리(flow 대기) 는 대상 범위 구분 없이 항상 별도 표기**(Markdown 하단 모니터링 섹션 / CSV 구분 열, 총계 불포함). 삭제 항목은 리포트에서 제외. *(v1.2 개정: 기존 '대상 범위 3종 선택'을 제거)* | 필수 |
| FR-18 | 실적 리포트 구성: 완료일 오름차순(시간순) 정렬, 카테고리 계층(대분류 → 소분류)으로 그룹핑, 그룹별 건수 집계 표시. 카테고리 미입력 Task는 "미분류" 그룹으로 출력 | 필수 |
| FR-19 | 실적 리포트 형식: Markdown(.md) 및 CSV 두 가지 지원, 저장 위치는 파일 저장 다이얼로그로 사용자 지정. Markdown 출력 예시: |  필수 |

```markdown
# 2026년 업무 실적 (flow 등록 기준, 총 147건)

## 통합정보망 (58건)
### 태양광 (14건)
- 03-12 | 과업내용서 초안 작성 (등록 03-05)
- 04-02 | 제안요청서 검토의견 회신 (등록 03-28)
### 인프라 (22건)
...

## 기관정보화 (31건)
### 그룹웨어 (18건)
...

## 미분류 (5건)
...
```

### 4.3 데이터 저장 경로
| ID | 요구사항 | 우선순위 |
|----|----------|:--:|
| FR-20 | 데이터 저장 위치는 앱이 **`%APPDATA%\TaskTray` 폴더에서 자동 관리**한다. 최초 실행 시 이 폴더와 `tasks.json`을 자동 생성하며, **사용자가 경로를 지정하지 않는다**(폴더 선택 다이얼로그 없음). *(v1.2 개정)* | 필수 |
| FR-21 | 저장 폴더는 앱이 고정 관리한다. 환경설정의 **[저장 폴더 열기]** 버튼으로 해당 폴더를 탐색기로 연다. 사용자 경로 변경 UI는 제공하지 않는다. *(v1.2 개정)* | 필수 |
| FR-22 | 실행 시 관리 폴더의 `tasks.json`을 읽어 동작. 파일이 이미 존재하면 그대로 로드(타 PC에서 폴더/파일을 복사해 온 시나리오 지원) | 필수 |
| FR-23 | JSON 파일 손상 대비: 저장 시 임시파일 기록 후 교체(atomic write), 직전 버전 1개를 `tasks.backup.json`으로 자동 백업 | 필수 |

### 4.4 환경설정
| ID | 요구사항 | 우선순위 |
|----|----------|:--:|
| FR-24 | 설정 항목: ① 배경 색상 ② 글자 색상 ③ 글자 크기 ④ 앱 창 크기(가로·세로 px) ⑤ Windows 시작 시 자동 실행 On/Off ⑥ **[저장 폴더 열기]** (경로 표시 + 탐색기 열기 버튼, 경로 지정은 없음) *(v1.2 개정)* ⑦ 제목 자동 분리(FR-04) On/Off | 필수 |
| FR-25 | 색상은 컬러 피커로 선택, 글자 크기는 숫자 입력 또는 단계(소/중/대) 선택 | 필수 |
| FR-26 | 설정 변경은 즉시 반영(재시작 불필요)되며 `settings.json`에 저장 | 필수 |
| FR-27 | 자동 실행은 Windows 시작프로그램 등록 방식(레지스트리 Run 키 또는 Tauri autostart 플러그인) 사용 | 필수 |
| FR-28 | 설정 초기화(기본값 복원) 버튼 제공 | 권장 |

---

## 5. UI/UX 요구사항 (UI)

| ID | 요구사항 |
|----|----------|
| UI-01 | 프로그램은 시스템 트레이에 상주. 트레이 아이콘은 task/체크리스트 콘셉트의 아이콘 사용 |
| UI-02 | 트레이 아이콘 좌클릭 → 화면 **우측 하단(트레이 인접)**에 메신저(카카오톡 PC) 크기의 세로형 패널 표시. 재클릭 또는 패널 외부 클릭(포커스 아웃) 시 숨김 |
| UI-03 | 패널 기본 크기: 약 가로 360px × 세로 720px (설정에서 변경 가능) |
| UI-04 | 트레이 아이콘 우클릭 → 컨텍스트 메뉴: [열기], [환경설정], [로우데이터 보기], [종료] |
| UI-05 | 패널 구성(위→아래): ① 빠른 입력창(항상 최상단, 카테고리 자동완성 필드 포함) ② 오늘 할 일(Pin) 영역 ③ 진행 중(active) 목록 ④ flow 등록 대기(done) 목록 — 각 영역은 접기/펼치기 가능 |
| UI-06 | Task 1행 구성: 완료 체크박스 + 제목 + 카테고리(축약 표시, 예: 소분류만) + 종료예정일(있을 경우) + Pin 토글 + (hover 시) 수정/삭제 아이콘. 1행 높이를 낮게 유지하여 한 화면에 다수 표시 |
| UI-07 | done 목록의 Task 1행에는 [flow 등록 완료] / [제외] 버튼(또는 체크) 노출 |
| UI-08 | 작업표시줄에는 표시하지 않음(트레이 전용). 항상 위(Always on top) 옵션은 v1 범위 제외 |
| UI-09 | 텍스트 위주의 미니멀 디자인. 애니메이션·이미지 최소화, 시스템 기본 폰트(맑은 고딕) 사용 |
| UI-10 | 창을 닫아도(X 또는 포커스 아웃) 프로그램은 종료되지 않고 트레이에 상주. 완전 종료는 트레이 우클릭 [종료]로만 수행 |

---

## 6. 데이터 요구사항 (DR)

### 6.1 tasks.json 스키마
```json
{
  "schemaVersion": 1,
  "tasks": [
    {
      "id": "uuid-v4",
      "title": "과업내용서 초안 작성",
      "category": "통합정보망_태양광",   // 선택값, 없으면 null. 구분자 '_'로 2뎁스 표현
      "status": "active",            // active | done | archived
      "pinned": false,                // 오늘 할 일 상단 고정 여부
      "dueDate": "2026-07-31",       // 선택값, 없으면 null
      "createdAt": "2026-07-24T09:12:00+09:00",
      "completedAt": null,
      "flowStatus": null,             // null | "registered" | "excluded"
      "flowProcessedAt": null,
      "deleted": false,
      "deletedAt": null
    }
  ]
}
```

### 6.2 settings.json 스키마 (사용자 프로필 폴더 %APPDATA% 하위에 저장)
```json
{
  "dataPath": "C:/Users/<user>/AppData/Roaming/TaskTray",  // v1.2: 앱 자동 관리(고정). 사용자 지정 아님
  "theme": {
    "backgroundColor": "#1e1e1e",
    "textColor": "#e0e0e0",
    "fontSize": 14
  },
  "window": { "width": 360, "height": 720 },
  "autoStart": true,
  "titleAutoParse": false
}
```

### 6.3 데이터 규칙
- DR-01 : 모든 시각은 ISO 8601 + KST(+09:00) 형식으로 기록
- DR-02 : 데이터 변경 즉시 저장(별도 저장 버튼 없음)
- DR-03 : `schemaVersion` 필드를 두어 향후 스키마 변경 시 마이그레이션 로직으로 대응
- DR-04 : JSON 파싱 실패 시 백업 파일 로드를 시도하고, 실패 사실을 사용자에게 알림
- DR-05 : 카테고리는 별도 마스터 데이터를 두지 않으며, tasks.json의 사용 값에서 실시간 추출하여 자동완성·필터·리포트에 활용

---

## 7. 비기능 요구사항 (NFR)

| ID | 요구사항 |
|----|----------|
| NFR-01 | **경량성**: 상주 시 메모리 점유 최소화(목표 100MB 이하), 트레이 클릭 후 패널 표시까지 1초 이내 |
| NFR-02 | **단일 인스턴스**: 프로그램 재실행 시 이미 실행 중인 인스턴스를 감지하여 **기존 인스턴스를 종료**하고 새 인스턴스만 실행. 버전이 다른 인스턴스(구버전)도 동일하게 종료 대상. 구현 방식: 버전과 무관한 고정 이름의 mutex/락파일 + 로컬 IPC(named pipe 등)로 기존 프로세스에 종료 신호 전송 |
| NFR-03 | **설치파일**: NSIS 기반 `.exe` 설치파일로 배포. per-user 설치(관리자 권한 불필요), 시작 메뉴 등록, 제거(uninstall) 지원 |
| NFR-04 | **버전관리**: 앱 버전을 Semantic Versioning(예: 1.0.0)으로 관리, 설정 화면 하단에 현재 버전 표시. 버전업 설치 시 기존 버전 위에 덮어쓰기 설치되며 데이터(JSON)는 유지 |
| NFR-05 | **오프라인**: 네트워크 연결 없이 모든 기능 동작. 외부 서버 통신 없음 |
| NFR-06 | **한국어 UI**: 모든 화면·메시지는 한국어 |
| NFR-07 | **안정성**: 비정상 종료 시에도 마지막 저장 시점 데이터 보존(DR-02 즉시 저장 + FR-23 atomic write로 보장) |

---

## 8. 개발 단계 (Claude Code 작업 지시)

Claude Code는 아래 마일스톤 순서로 개발하며, **각 단계 완료 시 실행 가능한 상태로 커밋**할 것.

| 단계 | 작업 내용 | 완료 기준 |
|:--:|----------|----------|
| M1 | 프로젝트 초기화: Tauri 2.x + TypeScript + Vite 스캐폴딩, 트레이 아이콘 상주 + 좌클릭 패널 토글 + 우클릭 메뉴 골격 | 트레이에서 빈 패널이 뜨고 닫힘 |
| M2 | 데이터 계층: 저장 경로 지정 다이얼로그, tasks.json/settings.json 읽기·쓰기, atomic write + 백업 | 재시작 후 데이터 유지 확인 |
| M3 | Task 핵심 기능: 등록/수정/삭제/완료, 카테고리 자동완성 및 계층 규칙, 제목 자동 분리 옵션, 종료예정일, Pin 고정, 목록 정렬 (FR-01~12) | 4.1 요구사항 전체 동작 |
| M4 | flow 워크플로우: done 목록, flow 등록 완료/제외 처리, archived 숨김, 되돌리기 | 3.2 상태 흐름 전체 동작 |
| M5 | 로우데이터 화면 + 실적 리포트: 전체 조회, 상태·카테고리 필터, 검색, 폴더 열기, 실적 리포트 내보내기(Markdown/CSV, 카테고리 계층 그룹핑) (FR-13~19) | 4.2 요구사항 전체 동작 |
| M6 | 환경설정: 테마(색상·글자크기), 창 크기, 자동 실행, 경로 변경, 제목 자동 분리 On/Off (FR-24~28) | 설정 즉시 반영 확인 |
| M7 | 단일 인스턴스 + 패키징: 기존 인스턴스 종료 로직, NSIS 설치파일 빌드, 버전 표기 | 설치→실행→중복실행 시 기존 종료 확인 |

### 개발 시 준수사항
- 각 마일스톤 착수 전 구현 계획을 간단히 제시하고 진행할 것
- 요구사항이 모호하거나 상충하는 경우 임의 구현하지 말고 질문할 것
- README.md에 빌드 방법(`개발 실행`, `설치파일 빌드` 명령)을 정리할 것
- 커밋 메시지는 `[M단계] 작업 내용` 형식의 한국어로 작성할 것

---

## 9. 검수 기준 (Acceptance Criteria)

1. 트레이 아이콘 클릭 → 1초 이내 패널 표시, 외부 클릭 시 숨김, 프로그램은 상주 유지
2. Task 등록 → 재시작 후에도 유지되고, 지정 폴더의 tasks.json에서 사람이 읽을 수 있는 형태로 확인 가능
3. 카테고리 입력 시 기존 사용 값이 자동완성으로 추천됨 (예: "태" 입력 → "통합정보망_태양광" 추천). 새 값을 타이핑하면 별도 등록 절차 없이 즉시 새 카테고리로 사용됨
4. 완료 처리한 Task가 flow 등록 대기 목록에 나타나고, [flow 등록 완료] 또는 [제외] 체크 시 기본 화면에서 사라짐
5. 숨겨진(archived) Task를 로우데이터 화면에서 조회 가능하며, 상태·카테고리 필터와 검색이 동작함
6. 실적 리포트 내보내기: 기간과 대상 범위(flow 등록 완료만) 지정 시, 완료일 시간순 + 대분류/소분류 2뎁스 그룹핑 + 그룹별 건수가 포함된 Markdown 파일이 생성됨. CSV로도 동일 데이터 출력 가능
7. tasks.json을 다른 PC의 동일 관리 폴더(`%APPDATA%\TaskTray`)로 복사하면 동일하게 동작 *(v1.2 개정)*
8. 설정에서 배경색·글자색·글자크기·창 크기 변경 시 즉시 반영, 재시작 후에도 유지
9. Windows 시작 시 자동 실행 On 설정 후 재부팅하면 트레이에 자동 상주
10. 프로그램 실행 파일을 한 번 더 실행하면 기존 인스턴스가 종료되고 새 인스턴스 1개만 남음
11. NSIS 설치파일로 설치·제거 가능하며, 제거 후에도 사용자 지정 경로의 tasks.json은 삭제되지 않음
