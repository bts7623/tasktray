# CLAUDE.md — TaskTray 프로젝트 작업 원칙

## 0. 최우선 원칙 (반드시 준수)

1. **요구사항의 유일한 기준은 [`개인업무관리프로그램_요구사항정의서.md`](개인업무관리프로그램_요구사항정의서.md) 이다.**
   - 이 문서에 없는 기능은 **임의로 구현하지 않는다.**
   - 필요하다고 판단되는 추가 기능은 **구현하지 말고 "제안"만** 한다.
   - 문서와 코드가 어긋나면 문서가 옳다. 코드를 문서에 맞춘다.
2. **모호하거나 상충하는 요구사항은 임의 해석하지 않고 반드시 질문한다.**
   - 추측으로 구현한 뒤 사후 보고하는 방식 금지.
   - 질문 대기 중에도 해당 결정에 의존하지 않는 작업은 진행 가능.
   - 확정된 답변은 아래 §3 "확정 결정 사항"에 기록한다.
3. **마일스톤(M1~M7) 순서대로 개발한다.**
   - 각 단계 **착수 전** 구현 계획을 간단히 제시하고 **사용자 승인**을 받는다.
   - 각 단계 완료 시 **실행 가능한 상태로 커밋**하고 **실행/테스트 방법**을 안내한다.
4. **커밋 메시지는 `[M단계] 작업 내용` 형식의 한국어**로 작성한다. (예: `[M1] Tauri 스캐폴딩 및 트레이 패널 토글 구현`)
5. **README.md에 빌드 방법**(`개발 실행`, `설치파일 빌드`)을 정리하고 최신 상태로 유지한다.

## 1. 프로젝트 요약

- 이름: TaskTray — 개인 업무 스케줄 관리 프로그램
- 목적: ① 수시 업무 즉시 메모(누락 방지) ② 완료 업무의 flow(사내 SaaS) 등록 여부 추적 ③ 누적 실적 리포트 내보내기(KPI)
- 형태: Windows 시스템 트레이 상주형 초경량 앱. 트레이 좌클릭 시 화면 우측 하단에 세로형 패널(기본 360×720).
- 데이터: 로컬 JSON 파일(`tasks.json`) 1개 + 설정(`settings.json`). 완전 오프라인, 외부 통신 없음.
- 스택(요구사항 §2.1): Tauri 2.x + TypeScript + Vite. **스택 변경이 필요하면 사유를 먼저 보고 후 진행.**

## 2. 마일스톤

| 단계 | 작업 내용 | 상태 |
|:--:|----------|------|
| M1 | 프로젝트 초기화(Tauri+TS+Vite), 트레이 상주 + 좌클릭 패널 토글 + 우클릭 메뉴 골격 | 코드 완료(빌드 검증은 Build Tools 설치 후) |
| M2 | 데이터 계층: 저장 경로 지정, tasks/settings JSON 읽기·쓰기, atomic write + 백업 | 미착수 |
| M3 | Task 핵심 기능 (FR-01~12) | 미착수 |
| M4 | flow 워크플로우 (상태 흐름 §3.2) | 미착수 |
| M5 | 로우데이터 화면 + 실적 리포트 (FR-13~19) | 미착수 |
| M6 | 환경설정 (FR-24~28) | 미착수 |
| M7 | 단일 인스턴스 + NSIS 패키징 (NFR-02~04) | 미착수 |

## 3. 확정 결정 사항 (질문→답변 기록)

> 사용자 확답을 받은 항목만 기록한다. 미확정 항목은 §4에 둔다.

- **D-01 (스택)** : Tauri 2.x 유지. Rust + MSVC 빌드 툴체인을 이 PC에 설치하고 진행한다. Electron 대안 채택하지 않음.
- **D-02 (화면 구조)** : 창 3개 구성. ① 메인 패널(트레이 좌클릭, 360×720) ② 환경설정 = **별도 창** ③ 로우데이터 = **별도 창**. 메인 패널은 Task 전용으로 가볍게 유지.
- **D-03 (포커스 아웃)** : UI-02의 "패널 외부 클릭 시 숨김"은 **다이얼로그/자체 창으로 포커스가 넘어간 경우 억제**한다. 즉 네이티브 다이얼로그(폴더 선택·파일 저장), 컬러 피커, 앱 자신의 다른 창(설정·로우데이터)이 활성일 때는 패널을 숨기지 않는다. 완전히 다른 프로그램으로 포커스가 넘어갔을 때만 숨긴다.
- **D-04 (스키마)** : §6.1 tasks.json 스키마를 **문자 그대로 유지**한다. `updatedAt`, `archivedAt` 등 임의 필드를 추가하지 않는다. archived 전환 시각은 `flowProcessedAt`으로 대체한다.

## 4. 미확정/질문 대기 항목

> 이 항목들은 **답변 전까지 임의 구현 금지**. 해당 마일스톤 착수 시 반드시 재확인한다.

- [ ] (Q-05/M2) FR-20 최초 실행 시 저장 폴더 지정 다이얼로그의 표시 시점, 사용자가 취소했을 때의 동작
- [ ] (Q-06/M2) FR-21 경로 변경 시 새 경로에 이미 `tasks.json`이 존재하는 경우의 처리(덮어쓰기 / 기존 파일 사용 / 취소)
- [ ] (Q-07/M3) FR-04 제목 자동 분리 On 상태에서 카테고리 입력란에도 값이 있을 때의 우선순위
- [ ] (Q-08/M3) Pin On 상태인 Task를 완료(done) 처리했을 때 Pin 영역과 done 영역 중 어디에 표시할지
- [ ] (Q-09/M3) Pin 영역 내부의 정렬 기준 (FR-12은 Pin 영역 자체의 내부 순서를 정의하지 않음)
- [ ] (Q-10/M5) FR-17 대상 범위 "전체 완료"에 아직 flow 미처리 상태(`done`)인 Task도 포함할지, `deleted:true`인 완료 Task의 리포트 포함 여부
- [ ] (Q-11/M4) FR-11 종료 취소(archived→done)를 로우데이터 화면에서만 제공할지 (archived는 기본 화면에서 숨겨지므로)
- [ ] (Q-12/M2) DR-01 "모든 시각은 KST(+09:00)" — 시스템 타임존과 무관하게 항상 +09:00 고정 기록할지

## 5. 개발 환경 메모

- 개발 PC: Windows 11 Pro (x64)
- 설치 확인됨: Node v25.9.0 / npm 11.12.1 / pnpm 11.1.2 / git 2.53.0 / WebView2 Runtime 150.x
- 설치 완료: **Rust 1.97.1** (stable-x86_64-pc-windows-msvc)
- **미설치: Visual Studio Build Tools 2022 (VCTools + Windows SDK)** → `link.exe` 부재로 Rust 최종 링크 단계 실패. 사용자가 관리자 권한으로 직접 설치 필요 (winget 비대화형 설치는 UAC 취소로 실패, 종료코드 1602).
- pnpm 빌드 스크립트 승인: `pnpm-workspace.yaml`의 `allowBuilds: { esbuild: true }` (pnpm 11.1.2 방식)
