# CLAUDE.md — TaskTray 프로젝트 작업 원칙

## 0. 최우선 원칙 (반드시 준수)

1. **요구사항의 유일한 기준은 [`개인업무관리프로그램_요구사항정의서.md`](개인업무관리프로그램_요구사항정의서.md) 이다.**
   - 이 문서에 없는 기능은 **임의로 구현하지 않는다.**
   - 필요하다고 판단되는 추가 기능은 **구현하지 말고 "제안"만** 한다.
   - 문서와 코드가 어긋나면 문서가 옳다. 코드를 문서에 맞춘다.
2. **모호하거나 상충하는 요구사항은 임의 해석하지 않고 반드시 질문한다.**
   - 추측으로 구현한 뒤 사후 보고하는 방식 금지.
   - 질문 대기 중에도 해당 결정에 의존하지 않는 작업은 진행 가능.
   - 확정된 답변은 아래 §3 "확정 결정 사항"에 기록한다.
3. **마일스톤(M1~M7) 순서대로 개발한다.**
   - 각 단계 **착수 전** 구현 계획을 간단히 제시하고 **사용자 승인**을 받는다.
   - 각 단계 완료 시 **실행 가능한 상태로 커밋**하고 **실행/테스트 방법**을 안내한다.
4. **커밋 메시지는 `[M단계] 작업 내용` 형식의 한국어**로 작성한다. (예: `[M1] Tauri 스캐폴딩 및 트레이 패널 토글 구현`)
5. **README.md에 빌드 방법**(`개발 실행`, `설치파일 빌드`)을 정리하고 최신 상태로 유지한다.

## 1. 프로젝트 요약

- 이름: TaskTray — 개인 업무 스케줄 관리 프로그램
- 목적: ① 수시 업무 즉시 메모(누락 방지) ② 완료 업무의 flow(사내 SaaS) 등록 여부 추적 ③ 누적 실적 리포트 내보내기(KPI)
- 형태: Windows 시스템 트레이 상주형 초경량 앱. 트레이 좌클릭 시 화면 우측 하단에 세로형 패널(기본 360×720).
- 데이터: 로컬 JSON 파일(`tasks.json`) 1개 + 설정(`settings.json`). 완전 오프라인, 외부 통신 없음.
- 스택(요구사항 §2.1): Tauri 2.x + TypeScript + Vite. **스택 변경이 필요하면 사유를 먼저 보고 후 진행.**

## 2. 마일스톤

| 단계 | 작업 내용 | 상태 |
|:--:|----------|------|
| M1 | 프로젝트 초기화(Tauri+TS+Vite), 트레이 상주 + 좌클릭 패널 토글 + 우클릭 메뉴 골격 | ✅ 완료(빌드/링크 검증됨) |
| M2 | 데이터 계층: 저장 경로 지정, tasks/settings JSON 읽기·쓰기, atomic write + 백업 | ✅ 완료(빌드+단위테스트 5종 통과) |
| M3 | Task 핵심 기능 (FR-01~12) | ✅ 완료(빌드+프론트 테스트 18종 통과) |
| M4 | flow 워크플로우 (상태 흐름 §3.2) | ✅ 완료(빌드+프론트 테스트 22종 통과) |
| M5 | 로우데이터 화면 + 실적 리포트 (FR-13~19) | ✅ 완료(빌드+프론트 테스트 35종 통과) |
| M6 | 환경설정 (FR-24~28) | ✅ 완료(빌드 통과, 자동실행/즉시반영 구현) |
| M7 | 단일 인스턴스 + NSIS 패키징 (NFR-02~04) | 미착수 |

## 3. 확정 결정 사항 (질문→답변 기록)

> 사용자 확답을 받은 항목만 기록한다. 미확정 항목은 §4에 둔다.

- **D-01 (스택)** : Tauri 2.x 유지. Rust + MSVC 빌드 툴체인을 이 PC에 설치하고 진행한다. Electron 대안 채택하지 않음.
- **D-02 (화면 구조)** : 창 3개 구성. ① 메인 패널(트레이 좌클릭, 360×720) ② 환경설정 = **별도 창** ③ 로우데이터 = **별도 창**. 메인 패널은 Task 전용으로 가볍게 유지.
- **D-03 (포커스 아웃)** : UI-02의 "패널 외부 클릭 시 숨김"은 **다이얼로그/자체 창으로 포커스가 넘어간 경우 억제**한다. 즉 네이티브 다이얼로그(폴더 선택·파일 저장), 컬러 피커, 앱 자신의 다른 창(설정·로우데이터)이 활성일 때는 패널을 숨기지 않는다. 완전히 다른 프로그램으로 포커스가 넘어갔을 때만 숨긴다.
- **D-04 (스키마)** : §6.1 tasks.json 스키마를 **문자 그대로 유지**한다. `updatedAt`, `archivedAt` 등 임의 필드를 추가하지 않는다. archived 전환 시각은 `flowProcessedAt`으로 대체한다.
- **D-05 (최초 폴더 지정)** : 첫 실행 시 **즉시 네이티브 폴더 선택 다이얼로그**를 띄운다. 사용자가 취소하면 **기본 경로 `%APPDATA%\TaskTray`** 를 자동 사용하고 진행한다(이후 설정에서 변경 가능). 다이얼로그 표시 중에는 패널 포커스 아웃 숨김을 억제한다(D-03).
- **D-06 (시각 타임존)** : 모든 시각은 시스템 타임존과 무관하게 **항상 KST(+09:00) 고정**으로 기록한다. (예: `2026-07-28T15:30:00+09:00`)
- **D-07 (settings 저장)** : `settings.json`도 **atomic write**(임시파일→교체)로 저장한다. 단 백업 파일(`settings.backup.json`)은 만들지 않는다. `tasks.json`은 요구사항대로 atomic write + `tasks.backup.json` 백업.
- **D-08 (저장 위치 자동 관리)** : 사용자 요청으로 FR-20/21/22를 변경한다. **최초 실행 폴더 선택 다이얼로그를 없애고**, 모든 데이터(`tasks.json`·`tasks.backup.json`·`settings.json`)를 **`%APPDATA%\TaskTray` 고정 폴더에서 앱이 자동 관리**한다. 사용자는 경로를 인식·지정하지 않는다. 접근은 **환경설정의 [저장 폴더 열기] 버튼**으로 제공. 이 위치는 프로그램 제거·재설치·버전업 후에도 유지되어 NFR-04·검수 11을 충족한다(설치 폴더 내부에 두지 않는 이유). 요구사항정의서 FR-20/21, FR-24, §1.3, 검수 7을 이에 맞춰 갱신함. D-05(폴더 선택 다이얼로그)는 이 결정으로 폐기.
- **D-09 (Q-07 자동분리 우선순위)** : FR-04 제목 자동분리 On 상태에서 제목에 `_`가 포함되고 카테고리 입력란에도 값이 있으면 **자동분리 결과(제목의 마지막 `_` 앞부분)를 카테고리로 우선** 사용하고, 카테고리 입력란 값은 무시한다.
- **D-10 (Q-08 Pin된 완료 Task)** : Pin(오늘 할 일) On 상태인 Task를 완료(done) 처리하면 **done(flow 등록 대기) 영역으로 이동**한다. Pin 영역에는 `pinned && status==active` 인 Task만 표시한다.
- **D-11 (Q-09 Pin 영역 정렬/표시)** : Pin(오늘 할 일=별도 지정) 영역은 **최상단에 별도 음영 처리**로 표시하고, 영역 내부는 **카테고리를 포함한 문자열 기준 가나다(ko) 순** 정렬한다. active(진행 중)·done(대기) 영역은 FR-12 그대로(각각 등록일·완료일 최신순) 유지한다. ※ "별도 지정한 task = Pin된 Task"로 해석함(M3 계획에서 사용자 확인).
- **D-12 (Q-11 종료취소 위치)** : flow 처리(등록완료/제외)로 archived 전환 **직후 패널 하단에 '실행취소' 스낵바**를 몇 초간 노출해 즉시 되돌릴 수 있게 한다(오조작 복구). 이후의 archived→done 되돌리기는 **로우데이터 화면(M5)** 에서 제공한다. archived Task는 기본 패널에 노출하지 않는다(§3.2 숨김 유지).
- **D-13 (Q-10 리포트 대상/표기)** : FR-17 실적 리포트 규칙 —
  · **KPI 총계는 flow 등록완료(registered) 기준**.
  · **제외(excluded)** 와 **미처리(done, flow 대기)** 는 **대상 범위 구분 없이 항상** 표기한다: Markdown은 **하단 별도 모니터링 섹션**('제외(모니터링)', 'flow 미처리(모니터링)'), CSV는 **구분 열**(등록완료/제외/미처리)로 구분하고 **제외·미처리 행을 아래쪽에 정렬**. 둘 다 **총계 불포함**.
  · **삭제(deleted:true)** 완료 Task는 **리포트에서 완전 제외**. 단 로우데이터 표에서는 `deleted` 필터로 조회 가능.
  · **D-13 개정(사용자 요청)**: 최초엔 대상 범위 3종(등록완료만/제외 포함/전체 완료) 선택으로 포함 여부를 제어했으나, "제외는 항상 모니터링하고 싶다"는 요청에 따라 **대상 범위 드롭다운을 제거**하고 제외·미처리를 항상 별도 표기하도록 변경. 리포트 옵션은 **완료일 기간만** 지정. 요구사항 FR-17 갱신함.

- **D-14 (테마 프리셋, 사용자 요청)** : 환경설정 화면 테마에 **프리셋 8종**을 추가한다 — 밝은/다크/보라/초록(톤 테마), 선명/종이/야간/하늘(표·정보 가독성 위주). 각 2글자 라벨의 작은 칩 버튼을 '화면 테마' 제목 우측에 한 줄로 나열(세로 공간 절약). 프리셋 클릭 시 배경·글자 색상 즉시 적용, 이후 컬러픽커 수동 조정도 가능. settings 스키마 변경 없음(backgroundColor/textColor 값만 세팅). 환경설정 창 세로 확대(500×780)·스크롤 허용. FR-24 편의 확장.

## 4. 미확정/질문 대기 항목

> 이 항목들은 **답변 전까지 임의 구현 금지**. 해당 마일스톤 착수 시 반드시 재확인한다.

- [x] (Q-05/M2) FR-20 최초 실행 다이얼로그 시점·취소 동작 → **D-05로 확정**
- [~] (Q-06/M6) FR-21 경로 변경 시 파일 충돌 처리 → **D-08로 폐기**(경로 변경 UI 자체가 없어짐, 저장 위치 자동 관리)
- [x] (Q-13/M6) 글꼴(폰트 종류) 선택 추가 여부 → **추가하지 않음**(요구사항·UI-09대로 맑은 고딕 고정)
- [x] (Q-07/M3) FR-04 제목 자동 분리 우선순위 → **D-09로 확정**
- [x] (Q-08/M3) Pin On 상태인 완료 Task 표시 위치 → **D-10으로 확정**
- [x] (Q-09/M3) Pin 영역 내부 정렬/표시 → **D-11로 확정**(가나다 + 음영, 별도지정=Pin으로 해석)
- [x] (Q-10/M5) FR-17 대상 범위·제외/삭제 표기 → **D-13으로 확정**(전체완료=done+archived, 제외=별도섹션·총계제외, 삭제=리포트 제외)
- [x] (Q-11/M4) FR-11 종료 취소(archived→done) 제공 위치 → **D-12로 확정**(즉시 실행취소 스낵바 + 로우데이터 M5)
- [x] (Q-12/M2) DR-01 "모든 시각은 KST(+09:00)" — 항상 +09:00 고정 → **D-06으로 확정**

## 5. 개발 환경 메모

- 개발 PC: Windows 11 Pro (x64)
- 설치 확인됨: Node v25.9.0 / npm 11.12.1 / pnpm 11.1.2 / git 2.53.0 / WebView2 Runtime 150.x
- 설치 완료: **Rust 1.97.1** (stable-x86_64-pc-windows-msvc)
- 설치 완료: **Visual Studio Build Tools 2022** (`BuildTools\VC\Tools\MSVC\14.44.35207`, link.exe 확인) → `cargo build` 링크까지 정상 완료(exit 0).
- ⚠️ 주의: Rust/Build Tools 설치 **전에 열려 있던 터미널**은 PATH에 cargo가 없어 `pnpm tauri dev`가 실패한다(`cargo metadata ... program not found`). **새 터미널**을 열어야 함.
- pnpm 빌드 스크립트 승인: `pnpm-workspace.yaml`의 `allowBuilds: { esbuild: true }` (pnpm 11.1.2 방식)
