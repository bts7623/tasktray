{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "메인 패널·환경설정·로우데이터 창 공통 권한",
  "windows": ["main", "settings", "rawdata"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
