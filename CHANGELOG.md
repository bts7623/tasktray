# 변경 이력 (CHANGELOG)

TaskTray의 릴리스별 변경 사항을 누적 기록합니다. 최신 버전이 위에 옵니다.
각 항목의 `(D-xx)`는 [CLAUDE.md](CLAUDE.md) §3의 확정 결정 번호, 요구사항 근거는
[개인업무관리프로그램_요구사항정의서.md](개인업무관리프로그램_요구사항정의서.md)를 참조하세요.

> 형식: [Keep a Changelog](https://keepachangelog.com/) 간소화. 버전 체계: [SemVer](https://semver.org/).
> 배포물: 데스크톱 설치파일은 GitHub Releases, 웹/모바일(PWA)은 Vercel(`tasktray.vercel.app`).

---

## [1.5.0] — 2026-08-20 — 웹 접속 링크 + 사용자 피드백

- **추가** 환경설정 → 클라우드 동기화 상단에 **웹·모바일 접속 주소**(`tasktray.vercel.app`) + [열기]·[복사] 버튼. (D-23)
- **추가** **사용자 피드백 창구**: 로그인 사용자가 유형(버그/제안/기타) + 내용을 제출. 데스크톱=환경설정 하단, 웹=헤더 [피드백] 모달. (D-23)
- **추가** **관리자 열람**: `admins`에 등록된 계정만 전체 피드백을 열람·상태 토글(open/done). 데스크톱=[피드백 관리] 별도 창, 웹=오버레이.
- **보안** Supabase RLS가 방어선(일반 사용자는 자기 것 INSERT만, 관리자만 SELECT/UPDATE). **하루 5건 제한**은 서버 트리거로 KST 자정 기준 강제.
- 백엔드: `supabase/feedback.sql`(사용자가 SQL Editor에서 실행, 관리자 이메일 지정). 코드: `src/feedback/`.
- **주의** D-22와 동일하게 NFR-05(완전 오프라인) 완화의 연장.

## [1.4.0] — 2026-08-18 — 모바일·다기기 동기화

- **추가** 클라우드 동기화(Supabase): 환경설정 → "클라우드 동기화"에서 이메일/비밀번호 로그인 후 다기기 데이터 공유. (D-22)
- **추가** 자동 동기화: 데스크톱(시작·15초 주기·패널 포커스·변경 후 2.5초), 웹(8초 주기·앱 복귀 즉시·Supabase Realtime 즉시). 로컬 우선이라 오프라인에서도 정상.
- **추가** 모바일 웹앱(PWA): 폰 브라우저 접속 → 홈 화면 설치 → 반응형 화면으로 동일 데이터 사용. `vite-plugin-pwa`.
- **변경** 데이터 스키마 v2: Task에 `updatedAt` 추가(동기화 충돌 판정). v1 파일은 로드 시 자동 마이그레이션. 모든 변경에서 `touch()`로 갱신. (기존 D-04 스키마 고정을 이 목적에 한해 개정)
- **주의** 요구사항 NFR-05(완전 오프라인)를 의식적으로 완화(사용자 승인). 데이터는 여전히 로컬(`%APPDATA%\TaskTray`) 우선 저장 + 로그인 시 클라우드 동기화.
- 백엔드: `supabase/schema.sql`(tasks 테이블 + RLS + Realtime), 키는 `.env.local`(git 제외) 및 Vercel 환경변수.

## [1.3.0] — 2026-08-12 — 정렬 개편

- **변경** Pin(오늘 할 일)·진행 중 목록 정렬을 **대분류(1depth) 가나다 → 소분류(2depth) 가나다 → task명 가나다**로. 같은 대분류 안에서 소분류끼리 묶인 뒤 제목순. done(flow 대기)은 완료일 최신순 유지. (D-21 / FR-12 개정)

## [1.2.0] — 2026-08-10 — 편의 개선 + 버그 수정

- **추가** 패널 헤더 **환경설정(⚙, 테마 글자색 연동 SVG)** 버튼(순서: 투명도 › ⚙ › 압정). (D-20)
- **추가** 제목 'TaskTray' 클릭 → 사용 설명서 열기.
- **추가** 카테고리 칩 클릭 → 색상 변경, 같은 대분류 일괄 적용·저장(`settings.categoryColors`).
- **추가** 종료예정일 요일 표기(예: `~08-07(목)`).
- **개선** 날짜 입력 달력 아이콘이 밝은/어두운 테마에 맞게 보이도록(`--calendar-scheme`).
- **수정** 헤더 환경설정 버튼으로 창 열 때 앱이 멈추던 **메인 스레드 데드락**(창 열기 커맨드를 async + `run_on_main_thread`로).

## [1.1.0] — 2026-08-07 — 접근성·창 편의

- **추가** 글로벌 단축키(기본 `Ctrl+Alt+Space`, 설정에서 변경, 충돌 감지). (D-15)
- **추가** 압정(📌) 고정: 항상 위 + 바깥 클릭해도 유지. (D-16)
- **추가** 카테고리 색상: 대분류 해시 기반 자동 색(같은 대분류=같은 색). (D-17)
- **추가** 헤더 드래그로 창 이동(멀티모니터), 위치 저장·복원. (D-18)
- **추가** 창 투명도 슬라이더(헤더, 30~100%), 값 저장. (D-19)

## [1.0.0] — 2026-08-07 — 최초 정식 릴리스

- **M1~M7 완성**: 트레이 상주 + 좌클릭 패널 토글 + 우클릭 메뉴 / 데이터 계층(atomic write + 백업, 손상 복구) / Task 등록·수정·삭제·완료·Pin·카테고리·종료예정일(FR-01~12) / flow 워크플로우(done→archived, 실행취소) / 로우데이터 조회·필터·검색 + 실적 리포트(Markdown·CSV) / 환경설정(테마·글자·창크기·자동실행·제목분리·초기화) / 단일 인스턴스 + NSIS per-user 설치. (D-01~D-13)
- **추가** 테마 프리셋 9종(선명·밝은·종이·하늘·야간·태섭·다크·초록·보라). (D-14)
- **추가** 사용 설명서 창, 트레이 [TaskTray 제거] 버튼, 설치 시 설명 문구(Made by BTS).
- **결정 변경** 저장 위치를 사용자 지정 → `%APPDATA%\TaskTray` 앱 자동 관리(제거·재설치·버전업 후 데이터 유지). (D-08)
- **결정 변경** 실적 리포트: 제외·미처리를 항상 별도 섹션/구분 열로 표기, 삭제는 리포트 제외. (D-13)

---

## 다음(미출시) 후보

- 모바일 오탭 방지(핀·삭제 버튼 간격·크기)
- 웹에 로우데이터·실적 리포트
- 웹 테마·설정
- 오프라인 편집 큐잉·삭제 tombstone 정리
- 정식 네이티브 앱(iOS 푸시 알림 등, Capacitor/Tauri Mobile)
