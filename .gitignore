# 의존성
node_modules/

# 프론트엔드 빌드 산출물
dist/
dist-ssr/

# Rust / Tauri 빌드 산출물
src-tauri/target/
src-tauri/gen/schemas/

# 로그·에디터·OS
*.log
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Claude Code 로컬 설정
.claude/settings.local.json

# 환경변수(Supabase 키 등) — 로컬 전용
.env.local
.env*.local

# 업데이터 서명 개인키 — 절대 커밋 금지(분실 시 자동 업데이트 불가, 백업 필수)
tasktray-updater.key
# 공개키(.pub)는 tauri.conf.json 에도 들어가므로 참고용으로만 추적
